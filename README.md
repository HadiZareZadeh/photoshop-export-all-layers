# photoshop-export-all-layers
Use this code to export all layers in photoshop (2024 tested)

# Usage:
Just open your psd file in photoshop and go to File -> Script -> Browse, then select the export_layers.jsx file.